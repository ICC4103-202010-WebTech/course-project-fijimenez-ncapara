<p id="notice"><%= notice %></p>

<table>
  <thead>
  <tr>
    <th colspan="7"></th>
  </tr>
  </thead>

  <tbody>
  <tr>
    <td> description</td>
    <td> reply_id</td>
    <td> user_id</td>
    <td> event_id</td>
  </tr>
  <%@comments.each do |v|%>
  <tr>
    <td><%= v.description %></td>
    <td><%= v.reply_id %></td>
    <td><%= v.user_id %></td>
    <td><%= v.event_id %></td>
    <td><%= link_to 'Edit', edit_comment_path(v) %> |
      <%= link_to 'Back', events_path %></td>
  </tr>
    <%end %>
  </tbody>
</table>
