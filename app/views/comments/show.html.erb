<p id="notice"><%= notice %></p>

<table>
  <thead>
  <tr>
    <th colspan="7"></th>
  </tr>
  </thead>

  <tbody>
  <tr>
    <td> description</td>
    <td> reply_id</td>
    <td> user_id</td>
    <td> event_id</td>
    <td> comment_id</td>
  </tr>
  <%@comments.each do |v|%>
  <tr>
    <td><%= v.description %></td>
    <td><%= v.reply_id %></td>
    <td><%= v.user_id %></td>
    <td><%= v.event_id %></td>
    <td><%= v.id %></td>
    <td><%= link_to 'Back', "http://localhost:3000" %></td>
  </tr>
    <%end %>
  </tbody>
</table>
