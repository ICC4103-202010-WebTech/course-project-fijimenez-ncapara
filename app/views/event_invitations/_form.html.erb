<%= form_with(model: event_invitation, local: true) do |form| %>
  <% if event_invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_invitation.errors.count, "error") %> prohibited this event_invitation from being saved:</h2>

      <ul>
        <% event_invitation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :user %>
    <%= form.collection_select(:user_id, User.all.where(id: @eiu.id), :id, :name, {}, { class: "form-control" }) %>
  </div>
  <div class="form-group">
    <%= form.label :event %>
    <%= form.collection_select(:event_id, Event.all.order(name: :asc), :id, :name, {}, { class: "form-control" }) %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
